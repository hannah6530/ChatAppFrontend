import React, { useState } from 'react';
import './style.css';

const Profile = (props) => {

    return(
        <React.Fragment>
            hello this is profile
        </React.Fragment>

    );

};
export default Profile;